# Runtime dependencies for poly-web3 (SDK + analyzer + watcher).
# Install into a venv only — do not install this repo as a package:
#   pip install -r requirements.txt
# Then run with PYTHONPATH pointing at the repository root (see README.md).
eth-utils==5.3.1
fastapi>=0.115.0
httpx>=0.28.1
jinja2>=3.1.6
loguru>=0.7.3
py-builder-relayer-client>=0.0.1
py-clob-client>=0.25.0
pydantic>=2.11.0
rich>=14.2.0
setuptools>=80.9.0
uvicorn>=0.35.0
web3>=7.0.0,<8
websocket-client>=1.8.0
